/**
 * 查找数组元素出现的位置 indexOf 从头到尾开始查找
 * 参数:数组元素       返回值:返回第一次数组元素出现的索引或-1(找不到)      修不修改原数组:不修改
 * 
 * 参数:数组元素 index(从下标开始查询)      返回值:返回第一次数组元素出现的索引或-1(找不到)      修不修改原数组:不修改
 */
// var arr = [11, 22, 11, 32, 56, 11];
// var res = arr.indexOf(11); // 0
// var res = arr.indexOf(66); // -1
// console.log(res, arr);

// var res = arr.indexOf(11, 3); // 5(从索引3(32)开始 找到 最后一个11的位置)
// console.log(res, arr);

/**
 * lastIndexOf 从尾部开始查找数组元素
 * 参数:数组元素    返回值:返回从尾部第一次查到数组元素的下标或者-1(没找到)    修不修改原数组:不修改
 * 
 * 参数:数组元素 index    返回值:返回从尾部第一次查到数组元素的下标或者-1(没找到)    修不修改原数组:不修改
 */
// var arr = [11, 22, 32, 56, 11, 66, 11];
// var res = arr.lastIndexOf(11); // 6
// console.log(res, arr);

// var res = arr.lastIndexOf(11, 4); // 4
// console.log(res, arr);

